{
  "name": "@class-comfyui/database",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "esbuild src/migrate.ts src/seed.ts src/backup.ts src/workers.ts --bundle --platform=node --format=cjs --outdir=dist --out-extension:.js=.cjs",
    "migrate": "tsx src/migrate.ts",
    "seed": "tsx src/seed.ts",
    "backup": "tsx src/backup.ts",
    "workers": "tsx src/workers.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "tsx": "^4.19.1",
    "esbuild": "^0.28.2"
  }
}
